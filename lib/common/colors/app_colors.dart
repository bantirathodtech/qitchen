// Define colors for the app
import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primaryColor = Color(0xFF000000); // Primary Color (e.g., app bar, buttons)
  // static const Color primaryColor = Color(0xFF007AFF); // Primary Color (e.g., app bar, buttons)
  static const Color accentColor = Color(0xFFFFD700); // Accent Color (e.g., highlights, icons)

  // Text colors
  static const Color textColor = Color(0xFF333333); // Text Color (e.g., body text)
  static const Color headingColor = Color(0xFF000000); // Heading Color (e.g., titles, headings)

  // Background colors
  static const Color backgroundColor = Color(0xFFF6F6F6); // Background Color (e.g., page background)
  static const Color cardColor = Colors.white; // Card Color (e.g., container backgrounds)

  // Divider color
  static const Color dividerColor = Color(0xFFE0E0E0); // Divider Color (e.g., separate sections)

  // Icon color
  static const Color iconColor = Color(0xFF757575); // Icon Color (e.g., iconography)

  // Inactive color
  static const Color inactiveColor = Color(0xFF9E9E9E); // Inactive Color (e.g., disabled buttons)

  // Button color
  static const Color buttonColor = Color(0xFFFFD700); // Button Color (e.g., action buttons)// Yellow
//Orange 0xFFFF3D3D
  // Additional colors with comments describing their usage
  static const Color appBarColor = Color(0xFF007AFF); // App bar color
  static const Color tabBarColor = Color(0xFF007AFF); // Tab bar color
  static const Color snackbarColor = Color(0xFF333333); // Snackbar color
  static const Color bottomNavigationBarColor = Color(0xFFF6F6F6); // Bottom navigation bar color
  static const Color toggleSwitchOnColor = Color(0xFF007AFF); // Toggle switch on color
  static const Color toggleSwitchOffColor = Color(0xFF9E9E9E); // Toggle switch off color
  static const Color stepperColor = Color(0xFF000000); // Stepper color
  static const Color selectionColor = Color(0xFF007AFF); // Selection color
  static const Color highlightColor = Color(0xFFFFD700); // Highlight color
  static const Color tooltipColor = Color(0xFF000000); // Tooltip background color
}
